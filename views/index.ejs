<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>App per shkoll</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container mt-5">
    <h1 class="text-center">Chat App</h1>
    <form action="/room/<%= new Date().getTime() %>" method="get" class="mb-4">
        <input type="text" name="room" class="form-control" placeholder="Emri i room" required>
        <button type="submit" class="btn btn-primary mt-2">Krijo/Bashkohu ne room</button>
    </form>
    <h3>Dhomat ekzistuese</h3>
    <ul>
        <% rooms.forEach(room => { %>
            <li><a href="/room/<%= room %>"><%= room %></a></li>
        <% }) %>
    </ul>
</div>
</body>
</html>
